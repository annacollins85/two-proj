{
  "name": "two-proj",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:middleman && npm run build:nuxt && mv -v .build-nuxt/* .build/ && rm -rf .build-nuxt",
    "build:nuxt": "npm run build --prefix lang-urls",
    "build:middleman": "bundle exec middleman build",
    "install": "npm run install:middleman && npm run install:nuxt",
    "install:nuxt": "npm install --prefix lang-urls",
    "install:middleman": "BUNDLE_GEMFILE=./Middle/Gemfile bundle install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
